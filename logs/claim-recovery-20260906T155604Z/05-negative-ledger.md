# Receipt 05 — negative/open claim ledger

Date: `2026-09-06`  
Purpose: prevent passing mechanism tests from silently reviving claims their evidence does not support.

## Remains withdrawn: cleartext batch throughput superiority

The lane-distinct gate recovers evaluator correctness, not a speed win. The fair paired/native investigation, eval-only decomposition, and wider B=1,048,576 run found no useful cleartext crossover for the measured circuit class. The shared digest compressed a real evaluator deficit rather than causing it. Do not restore “faster than native HDL” or a batch-throughput crossover claim.

Existing receipts: `../helut-batch-crossover-negative-20260906T010000Z.md`, `../helut-paired-harness-optimization-20260906T000000Z.md`, and the native receipt directories already banked under `logs/`.

## Remains failed/open: native-dimension noisy PicoRV32

C60/C61's post-fix failures at native `n=N`, `k=7` are not repaired by C69's `n=512` full-adder functional gate, the circuit-scoped k=14 full-adder result, or the separate extract-to-KS `n=64` route. Do not describe native-dimension production PicoRV noisy SING as recovered.

## Remains a null: structural simplification versus ABC

The fresh scaling suite mapped the hand-designed and discovered 4-bit variants to 6 LUTs under the same `abc -lut 6` recipe. Existing structural-evolution receipts likewise report LUT/depth ties after identical ABC mapping. There is no area, depth, timing, frequency, power, or physical implementation win.

## Remains open: general search complexity/completeness

The sampled 8-bit fixture proves that one successful search trained on 64 of 65,536 rows and generalized under a full-domain SAT check. It does not prove asymptotic decoupling from input-space size. The phrase `search cost DECOUPLES from input space` printed by the test is not promoted as a general claim. Arbitrary topology and GA completeness remain open.

## Remains open: P1030680

Known-control garble, indel, and Future-bank passes do not establish target corruption. The current campaign ledger still has zero BREAK gates, no key, no scored plaintext, and no decrypt. Geometry-aware replay of 41 exact post-gap candidates and explicit corrected-letter replay remain unfinished; legacy garble/indel arms are partial. No global negative follows.

## Remains unmeasured: discrete-GPU superiority

No NVIDIA or AMD backend/hardware result exists. Apple unified memory is an architectural rationale, not a measured cross-platform win.

## Narrowly recovered, not generalized

- `helut-compile --validate`: one named three-tier Enigma control, not arbitrary compiler correctness.
- C69: functional equivalence at the exact one-vector path, not a noisy-BK confidence certificate.
- TensorLUT: bounded multi-LUT ripple-adder and one-DFF repair, not arbitrary synthesis.
- Bombe: known-control mechanism qualification, not target evidence.

Any episode edit made from this receipt set must preserve these boundaries verbatim or more conservatively.