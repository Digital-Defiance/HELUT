# E256-H H4 switching-sequence tranche satisfies its sealed adverse-outcome gate

This tranche executes the frozen arithmetic switching census, sequence-specific H3 collapse, four integrated repeated/sequence RTL variants, mutation controls, and generic mapped-cost comparison without promoting an H4 policy. The optimized census is exactly equivalent to all 1,474,560 frozen placements and reproduces the preregistered adverse outcome of 737,280 failures, including the first frozen-order witness. Functional, cycle, hierarchy, and deterministic-reproduction evidence close against the archived receipt. The architecture, audit, reproduction guide, and textbook preserve the OPEN/no-C/H/N boundary and make no physical-savings, Metal/FHE, or mixed-sequence security claim.

Watch for: no actionable findings (**confirmed**); the 737,280 failing windows validly park only the tested arithmetic family, while the negative generic LUT deltas remain separate-run ABC observations rather than physical area evidence.

**Verdict**: APPROVED

## High-level view

The structural corpus uses frozen `a`-then-`d` order and the executable corpus uses frozen `d`-then-`a` order. Reducing a placement to `(b=(a+s*d) mod 384,d)` is lossless: every class occurs ten times, so 147,456 class evaluations cover all 1,474,560 placements and preserve first-witness order.

The raw H3, Stage A, and canonical Stage B evaluators keep the prior-round selector on `B_(r-1)`, omit nonexistent `A_12`, and compare every boundary. RTL selectors sample only on a successful complete commit, restart at `w_0` for every block, and keep FF and rotating-ring keys aligned through E0 and twelve round edges.

Repeated variants start only on the 384 `d=0` schedules; sequence variants start on all 768 `d in {0,1}` schedules. Every selected input has byte-exact model evidence and a measured 12-cycle latency/13-cycle earliest initiation interval, with external selector pins changed after commit.

All nineteen controls traverse the relevant digest, evaluator, RTL/protocol, parser, hierarchy, state-boundary, or flatten-preservation path. Recursive/flat primitive accounting closes, and the extra 132 selector bits remain distinct from non-attributable LUT changes across independent ABC runs.

The receipt binds the sealed preregistration, marker-bounded contract, immutable predecessors, current H4 source hashes, pinned toolchain, and deterministic payload. `--check` compares the deterministic payload rather than the changing environment advisory; each archived complete record still validates its own full-record hash.

<details>
<summary>Issues (0)</summary>

No actionable findings.

</details>

<details>
<summary>Details</summary>

### Arithmetic-class reduction preserves the complete frozen census

`structural_records()` emits all 147,456 `(a,d)` schedules and checks the frozen digest; `executable_records()` emits `d=0` then `d=1` and checks its separate digest (`Scripts/e256_hardware_h4_sequence_gate.py:802-818,899-908`). The census evaluates each `(b,d)` once, multiplies by the proven ten placements per class, then scans `(a,d,start_round)` in frozen order for the first witness (`Scripts/e256_hardware_h4_sequence_gate.py:1134-1182`).

An independent enumeration counted 1,474,560 placements, 147,456 classes, and multiplicity ten; it reproduced 737,280 failures and `(a=0,d=1,start_round=1)`, indices `[1,2,3]`, output lane 0 missing `[6,11,24,29]`. The `column + offset` source direction agrees with the predecessor catalog and RTL (`Scripts/e256_hardware_candidate_gate.py:387-399`; `Hardware/RTL/Research/E256H/e256h_h4_sequence_core.v:31-51`).

### H3 collapse and integrated cores share the frozen boundaries

Effective masks implement `K_0=M_0 xor A_0`, interior `K_r=L_(w_(r-1))(B_(r-1)) xor M_r xor A_r`, and terminal `K_12=L_(w_11)(B_11) xor M_12` (`Scripts/e256_hardware_h4_sequence_gate.py:954-981`). Raw H3, moved-`B` Stage A, and canonical Stage B are separate evaluators; Stage B removes the next `A` only at comparable interior boundaries and never creates `A_12` (`Scripts/e256_hardware_h4_sequence_gate.py:999-1064,1195-1257`). The full check reproduced 221,184 comparisons per stage with zero mismatches.

The FF core reads `K_0` while idle and `K_(round+1)` while busy. The ring rotates on acceptance and each round, returning to `K_0` after thirteen rotations (`Hardware/RTL/Research/E256H/e256h_h4_sequence_core.v:103-178,225-304`; `Hardware/RTL/Research/E256H/e256h_h2_integrated_core.v:382-414`). Both selectors latch on `commit_accept`; every accepted block resets `round_index`. The testbench changes selector pins after commit, runs eight blocks per configuration, rejects early ready/busy release, and records earliest successor acceptance (`Hardware/Testbenches/Research/E256H/e256h_h4_sequence_core_tb.v:151-194,304-431`).

The parser requires exact row order/run masks and `-` in unexecuted repeated slots, while grading reconstructs exact input sets rather than accepting totals (`Scripts/e256_hardware_h4_sequence_gate.py:1365-1594`). Counts close at 9,216 per repeated top, 18,432 per sequence top, 55,296 model/cycle observations, 27,648 storage comparisons, and 18,432 bridges.

### Nineteen controls reach their validity paths

Selector-order, held-`w_0`, index, direction, key-address, byte-packing, ring-rotation, and live-pin defects execute through the shared RTL/testbench/parser and are compared with both planted and correct models (`Scripts/e256_hardware_h4_sequence_gate.py:1610-1838`). Collapse transport reaches the canonical boundary evaluator; terminal `A_12` fails closed in the effective-mask model. Incomplete configuration reaches the inherited protocol, while extra output and missing timing mutate real result lines before the normal parser.

Externalized state, hierarchy multiplicity, duplicate accounting, and post-map corruption reach the graded hierarchy/state/flatten validators (`Scripts/e256_hardware_h4_sequence_gate.py:1929-2125,2188-2235`). The archived receipt contains the exact nineteen IDs, and full reproduction again reported `19/19 detected`.

### Generic mapping is measured without physical attribution

Each top is synthesized independently before analysis-only flattening. Recursive child multiplicities and primitive metric/signature/type multisets must match the independent flat expansion; one loop-free `ltp -noff` result supplies generic combinational depth (`Scripts/e256_hardware_h4_sequence_gate.py:1889-2045`). State accounting isolates 3,328 mask bits, 12 versus 144 selector bits, seven protocol bits, and 518 datapath/control bits (`Scripts/e256_hardware_h4_sequence_gate.py:1929-1975`).

All banked surfaces explicitly reject reading `-407` or `-57` LUT6 as physical area, vendor utilization, Fmax, or an architecture winner (`directives/e256-hardware-architecture.md:948-949`; `directives/e256-audit.md:355-357`; `REPRODUCE.md:818-826`; `textbook/chapters/pillar-iii.tex:36-49`).

### Receipt identity and prose remain inside the OPEN boundary

Integrity validation re-hashes the preregistration, H4 marker, predecessors, H2 sources, certified list, material/S-box anchors, and exact toolchain. Current H4 hashes enter the payload; `check_prior()` validates the stored record before requiring byte-identical regenerated payload bytes/digest (`Scripts/e256_hardware_h4_sequence_gate.py:599-667,2456-2471`). `make e256-hw-h4-sequence-check` reproduced payload `4562482417cb2917208cf37f8c4441efbdde602230d878ba49d249b017822064` and ended in `CHECK PASS`.

Architecture, audit, REPRODUCE, and textbook state the same counterexample, populations, mapping caveat, and non-production scope (`directives/e256-hardware-architecture.md:939-949`; `directives/e256-audit.md:345-358`; `REPRODUCE.md:806-826`; `textbook/chapters/pillar-iii.tex:21-49`). The claim sheet, public site, and `\livingepoch` are unchanged; `make textbook` regenerated the bounded wording. No banked claim is stronger than the receipt.

</details>

<details>
<summary>File map</summary>

- `directives/e256-hardware-h4-sequence-preregistration.json` — sealed questions, corpora, protocol, controls, synthesis contract, and non-claims.
- `directives/e256-hardware-architecture.md` — frozen marker and OPEN result interpretation.
- `Hardware/RTL/Research/E256H/e256h_h4_sequence_core.v` — repeated/per-round cores and control fixtures.
- `Hardware/Testbenches/Research/E256H/e256h_h4_sequence_core_tb.v` — exact execution, timing, isolation, and protocol evidence.
- `Scripts/e256_hardware_h4_sequence_gate.py` — grading, controls, synthesis accounting, and emit/check.
- `logs/e256-hardware-h4-sequence-gate.json` — deterministic payload and self-hashed record.
- `directives/e256-audit.md`, `REPRODUCE.md`, `textbook/chapters/pillar-iii.tex` — bounded banking wording.
- `textbook/helut-living-textbook.md`, `textbook/helut-living-textbook.pdf` — regenerated textbook outputs.
- `Makefile` — emit/check entry points.

Full local change view: `git diff main` plus the untracked H4 files in `git status --short`.

</details>
