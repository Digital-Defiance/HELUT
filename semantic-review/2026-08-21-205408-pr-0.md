# Pinned, fail-closed Rust reference lane with adversarial E256 and DFF coverage

The task-8 fixes close all four prior medium findings. Root Make entry points enforce Rust 1.97.1, README commands enter the package for rustup discovery, checkout is full-SHA pinned, and the missing E256/DFF rejection paths now execute in tests. Watch for: no remaining high- or medium-severity findings.

**Verdict**: APPROVED

## High-level view

Every root Rust target sets `RUSTUP_TOOLCHAIN=1.97.1` and depends on an exact `rustc 1.97.1` check. Direct README commands first enter `Reference/Rust/`, and CI uses the same package-local model with checkout pinned to `11bd71901bbe5b1630ceea73d27597364c9af683` (v4.2.2).

E256 negative coverage now reaches profile/manifest drift, unknown and duplicate fields, strict lowercase line hex, table relationships, exact layout and symlinks, all four reserved-center branches, and first/final checkpoints. The integration verifier continues to bind 24 artifact paths and hashes, all 1,024 stream/trace rows, session/profile identity, reciprocal decrypt, and testbench parameters.

Yosys parity exhausts 8 full-adder rows, 16 counter transitions, and 1,024 toy-ISA transitions. Synthetic transitions execute plain DFF, active-low enable and reset, reset values zero and one, SDFFE reset-over-enable, and SDFFCE enable-gates-reset against the Swift contract.

Production Rust has no repository writer surface; test writes stay in temporary directories and Cargo output stays under ignored `build/rust-reference/`. Exact direct versions plus `Cargo.lock` pin the graph, while README/CLI wording keeps E256-050, production security, IND-CPA, and confidentiality claims open.

<details>
<summary>Issues (0)</summary>

No remaining high- or medium-severity findings.

</details>

<details><summary>Details</summary>

## Exact compiler and immutable checkout gates

A forced expected-version mismatch made `rust-reference-toolchain` exit before Cargo, while the normal root gate selected and accepted 1.97.1. Package-local commands use the directory containing `rust-toolchain.toml`; the workflow retains read-only permissions and a full checkout commit SHA rather than a mutable tag.

## Frozen E256 and Yosys edge behavior

The E256 tests call production validators and negatively mutate identity, hashes, schema fields, hex form/count, rotor/reflector relationships, directory entries, symlinks, branch inventory, and checkpoints. `make rust-reference-check` reports the full 1,024-byte/24-artifact KAT, reserved branches 4/4, reciprocal decrypt PASS, and the requested synthetic DFF polarity/reset/priority cases; full-adder, counter, and toy-ISA parity also pass.

## Consumer boundary, dependency graph, and hedges

Filesystem mutation calls occur only in the E256 temporary test fixture. Protected-tree path/status and content fingerprints for `Fixtures/`, `Hardware/`, and `Generated/` were identical before and after the aggregate gate, and CI independently checks tracked plus untracked writes. `cargo tree --locked --offline` resolved only the committed graph, and the user-facing text explicitly distinguishes internal three-track parity from the external KAT still required for E256-050.

</details>

<details>
<summary>File map</summary>

- `Makefile` — exact toolchain selection/check and aggregate Rust gates.
- `README.md` — package-local commands and bounded E256/security wording.
- `.gitignore` — Rust target-output exclusion.
- `.github/workflows/rust-reference.yml` — full-SHA checkout and protected-tree guard.
- `Reference/Rust/Cargo.toml`, `Cargo.lock`, `rust-toolchain.toml` — exact direct versions, locked transitives, and Rust 1.97.1.
- `Reference/Rust/src/e256.rs` — frozen KAT consumer and rejection tests.
- `Reference/Rust/src/yosys.rs` — Yosys evaluator and DFF runtime coverage.
- `Reference/Rust/src/lib.rs`, `src/main.rs`, `tests/reference_parity.rs` — read-only CLI/helpers and integration parity.

Full tracked diff: `git diff main -- .gitignore Makefile README.md`. The workflow and `Reference/Rust/` remain untracked and are listed by `git status --short --untracked-files=all`.

</details>
