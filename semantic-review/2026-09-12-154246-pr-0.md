# Shared-path exhaustive-corpus validation for E256-X0

Control 18 now removes a real record from the ordered-pair corpus used by the production algebraic census, replacing the synthetic count mismatch flagged in the prior review. The shared validator commits all 147,456 lexicographic identities, and the re-emitted receipt carries that evidence in its deterministic payload. Independent reconstruction and both reproduction entry points pass while the tranche remains OPEN structural evidence only. **Watch for:** None; the prior High is closed (**confirmed**), with no new semantic, determinism, control, or synchronization concern.

**Verdict**: APPROVED

## High-level view

Production first validates a concrete Cartesian corpus for shape, range, exact position-to-identity mapping, uniqueness, count, and a length-delimited aggregate, then grades that validated list. Completeness is therefore checked at the same boundary used by the algebraic census.

Control 18 deletes the middle record from that representation. The common validator rejects position 73,728 because `(192,0)` is absent and `(192,1)` is displaced into its slot, directly exercising the frozen “remove one ordered pair” mutation.

The corpus commitment is canonical payload data. Runner, raw receipt, payload, and archived-record identities match their synchronized assertions; no claim-sheet row, living epoch, architecture, policy, security value, or production authorization moved.

<details>
<summary>Issues (0)</summary>

No actionable findings.

</details>

<details>
<summary>Details</summary>

## Control 18 now crosses the production corpus boundary

The repaired implementation constructs and validates the Cartesian identities before algebraic grading (`Scripts/e256_x0_h4_evolution_gate.py:709-759`, `Scripts/e256_x0_h4_evolution_gate.py:764-765`). Exact `divmod(position, 384)` checks catch omission, insertion, reordering, and duplication at the first displaced position; count and uniqueness checks also catch a missing final record.

```python
filtered_pair_corpus = build_ordered_pair_corpus()
del filtered_pair_corpus[PAIR_COUNT // 2]
validate_ordered_pair_corpus(filtered_pair_corpus)
```

This is the preregistered mutation, not a parallel assertion (`Scripts/e256_x0_h4_evolution_gate.py:1480-1485`). It rejects with `ordered pair corpus identity at position 73728 mismatch: expected (192, 0), got (192, 1)` (`logs/e256-x0-h4-evolution-gate.json:1474-1478`). An independent invocation reproduced the valid 147,456/147,456 corpus and this rejection, closing the original High (**confirmed**).

## Corpus identity is deterministic payload evidence

The length-prefixed canonical identity aggregate is `09ea70b98a8eccc774cd86d9cd3033acbfcba0ee1c13ff1f06c6a686152c4b49` (`logs/e256-x0-h4-evolution-gate.json:10-13`), matched by a separate lexicographic reconstruction. The uniqueness set is not iterated into output, so it adds no hash-order dependence.

Independent canonicalization matched runner `d011419ca4a712101481162748c4fcafe960d733582ffd2e2fd635f7a99fd602`, raw receipt `8db5f21b6e0a7249fa9b558c02a989324911575e91704b5e096e2749744ac2c9`, payload `35060c802427046f5f4630c6b9db6401d8ac90c4649fc0cf088d3f35f31b1b44`, and archived record `3b77fa3747818ed974e39b67b54cbbeeeb6b442bf5781b4e7f846776a96c8110`. `make e256-x0-h4-evolution-check` and `python3 Scripts/e256_x0_h4_evolution_gate.py --self-test` pass. Cross-run equality correctly stops at canonical payload bytes and digest; elapsed time and repository state remain in the advisory envelope, while the archived envelope’s own record hash also recomputes.

## OPEN scope remains synchronized

The receipt keeps `OPEN_PROGRESS`, `claim_movement: false`, `OPEN_NOT_GRADED` for the mixed four-round bound, and the explicit E256-063/C/H/N/public-epoch non-claim (`logs/e256-x0-h4-evolution-gate.json:1372-1374`, `logs/e256-x0-h4-evolution-gate.json:2380-2417`). The contract, architecture capsule, audit, reproduction guide, and textbook scaffold retain the two closed SCCs, inability to cover all 16 supports, bounded eleven-transition interpretation, and exclusions for attack resistance, production policy, RTL enforcement, Metal/FHE, and physical FPGA evidence. Current identities are synchronized at `REPRODUCE.md:842`, `directives/e256-x0-h4-evolution.md:113`, `directives/e256-audit.md:129,362`, and `directives/e256-hardware-architecture.md:953`; superseded identities occur only in the preserved prior review. `directives/claim-sheet.md`, `textbook/preamble.tex`, and `site/` have no scoped diff, leaving the epoch at `2026-08-20 / C69`.

</details>

<details>
<summary>File map</summary>

- `Scripts/e256_x0_h4_evolution_gate.py` — validates the canonical pair corpus on production and control paths.
- `logs/e256-x0-h4-evolution-gate.json` — banks the corpus commitment and exact filtered-record rejection.
- `directives/e256-x0-h4-evolution.md` — updates repaired runner/receipt identities without promoting the result.
- `directives/e256-x0-h4-evolution-preregistration.json` — retains the frozen remove-one-pair control definition.
- `directives/e256-hardware-architecture.md`, `directives/e256-audit.md`, `REPRODUCE.md`, and `textbook/chapters/pillar-iii.tex` — synchronize payload evidence and non-claims.
- `Makefile` — exposes the validated emit/check entry points.

Full tracked tranche diff: `git diff main -- Makefile REPRODUCE.md directives/e256-audit.md textbook/chapters/pillar-iii.tex`. Core X0 files and the hardware architecture are untracked local additions and were reviewed in full. Prior pass: `semantic-review/2026-09-12-151835-pr-0.md`.

</details>
