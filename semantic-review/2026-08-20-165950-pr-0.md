# Schema-3 reciprocal center with a profile-bound 1,024-beat KAT

Task 4 replaces the historical high-fixed-point center with a reciprocal two-mode center, binds those semantics into the v2/gen0 profile, and regenerates the software, RTL, fixture, and TensorLUT evidence under fixture schema 3. The prior review’s Swift/RTL split and stale 36-byte schema-2 golden are resolved: fresh artifacts reproduce byte-for-byte, and the direct core plus both AXI table-loading paths match all 1,024 bytes. The calibrated equality run lands at 4,083/1,048,576 (z = 0.204), while the deliberately broken historical control lands at 100%. Closure remains blocked by contradictory historical-artifact identity.

Watch for: **confirmed, High** C39 still says the old round-one cone matches the now-revised `enigma256ScrambleFrag`; **confirmed, High** active Red scripts attach `g5`/`generation: 5` metadata to freshly synthesized v2/gen0/schema-3 artifacts.

**Verdict**: NEEDS_CHANGES

## High-level view

Schema 3 is one coherent executable profile. Its canonical identity includes fixture schema 3, reserved-pair reflector derivation, pre-step parity mode, center rule, map order, recurrence, and native NLFF, producing profile hash `2a9f54…`; contexts and machines retain that profile by value rather than consulting a mutable selector per byte.

The center and evidence paths agree on timing and behavior. A fixed-point-free base reflector reserves `0↔1`; parity one changes only that pair to two fixed points, and the same captured pre-step mask drives the center and state advance. The branch-covering golden forces both reserved inputs under both modes, verifies a staged 1,024-byte manifest before replacement, and drives direct RTL pre/post state parity plus AXIS and AXI-Lite ciphertext parity.

The remaining problem is provenance at the historical boundary. `e256_round1.v` correctly calls itself pre-schema-3, but C39 still equates its old UKW with the live schema-3 Swift fragment, while Red scripts synthesize current sources into generation-5-labelled evidence. The center implementation is ready for a bounded functional closure, but that contradiction prevents an honest E256-003 closure receipt.

<details>
<summary>Issues (2)</summary>

1. **C39 names the wrong implementation** (**confirmed, High**) — `e256_round1.v` deliberately retains the pre-schema-3 UKW, while C39 says it matches the task-4 `enigma256ScrambleFrag`. Point C39 at a frozen historical implementation/receipt and state explicitly that it does not exercise the schema-3 center.
2. **Current Red outputs are relabelled as gen5** (**confirmed, High**) — the scramble-fragment battery and wide campaign synthesize current v2/gen0/schema-3 sources but write `g5` filenames and `"generation": 5`. Retire those paths or derive every filename and ledger identity from the validated compatibility tuple.

</details>

<details><summary>Details</summary>

## Profile-bound center and pre-step timing

`Enigma256Generation` includes fixture schema 3, the reserved-pair rule, pre-step mode, and center map order in validation and canonical profile bytes (`Sources/HELUTCore/Enigma256Generation.swift:154-161`, `:285-303`, `:359-375`). The fixture, golden session, manifest, NLFF include, and checked-in TensorLUT derivatives agree on `E256/v2/gen0/2a9f54c…/fixture-v3`; the loader compares the complete identity before recomputing ciphertext (`Sources/HELUTCore/Enigma256Bridge.swift:304-412`). Contexts and machines store the selected profile/generation as immutable values (`Sources/HELUTCore/Enigma256Session.swift:80-83`; `Sources/HELUTCore/Enigma256.swift:257-260`). The bridge’s remaining global read is copied once before derivation (`Enigma256Bridge.swift:91-98`), so byte processing cannot switch profiles mid-stream. Broader removal of ambient research selectors and protocol-domain binding remains E256-005/E256-006 work, not part of this closure.

The center mode is XOR parity of the four enables in Swift and RTL (`Enigma256.swift:157-164`; `enigma_256_core.v:104`). The day-key builder reserves `0↔1` while pairing only values 2…255 (`Enigma256.swift:567-577`), and wiring validation rejects fixed points, a non-involution, or a different reserved pair. Mode one bypasses only center inputs zero and one (`enigma_256_core.v:124-128`). `processTraced` captures one mask, computes the center, then advances offsets and LFSR with that same mask (`Enigma256.swift:313-324`); RTL uses the same current enables on the accepting edge (`enigma_256_core.v:167-172`). Exhaustive byte-map checks establish a permutation, reciprocity, and exactly zero/two fixed points by mode; the 2,000-state sweep and separately hedged structural certificate pass without presenting those results as a security proof.

## Atomic branch-covering fixture and transport parity

The default plaintext constructor tracks `(mode,input)` and requires all of `0:0`, `0:1`, `1:0`, and `1:1` (`Sources/HELUTCore/Enigma256Bridge.swift:95-145`, `:446-452`). Bundle writing stages beside the destination, runs the full loader/trace/manifest verifier, and only then moves or replaces the directory (`Enigma256Bridge.swift:202-220`). The manifest pins the exact artifact set and SHA-256 for ten tables, duplicate plaintext/ciphertext forms, session, parameters, and eight trace streams (`Enigma256Bridge.swift:493-514`; `Fixtures/enigma256_golden/manifest.json`). A new temporary bundle and a second generation over that existing directory both matched the committed bundle byte-for-byte, exercising both promotion branches.

The direct testbench checks every beat’s pre/post LFSR, pre/post offsets, mask, center mode, center input/output, and ciphertext (`enigma_256_tb.v:41-49`, `:81-89`, `:119-159`). It passed all 1,024 beats, and the separate directed RTL test forced mode 0/1 with reserved input 0/1. AXIS burst and AXI-Lite each loaded all 2,560 table bytes and passed all 1,024 ciphertext bytes.

## Calibrated evidence and generated-artifact freshness

The equality test covers four deterministic keys and four nonces per key, with 65,536 zero bytes per stream. Its six-sigma gate around the stated independent-uniform null `1/256` observed 4,083/1,048,576 (`0.0038938522`, z `0.2035`); the quarantined all-fixed-point oracle returned equality for every observation (`Tests/HELUTTests/Enigma256Tests.swift:280-356`; `logs/e256-v2-gen0-center-equality.json`). The receipt labels this `ACCEPTED_BOUNDED_FUNCTIONAL_EVIDENCE` and disclaims IND-CPA proof, security estimates, external cryptanalysis, and production readiness. Modulo bias, zero-state policy, protocol domains, attack coverage, and independent implementation remain open; E256-003 would close only the fixed-point-center defect.

`e256_nlff_emit.py --check` validates the native-NLFF receipt. Yosys regenerated all five active cones; the NLFF and step-cone hashes exactly match their checked-in stamps (`enigma_256_tensorlut_baseline.v:2-4`; `enigma_256_step_cone_tensorlut.v:2-4`), and fresh emitter runs were byte-identical to both files. The center-bearing fragment also synthesized from current source with a schema-3 compatibility/source stamp. This resolves the prior stale schema-2 generated artifacts rather than merely changing header text.

## Historical evidence identity is still contradictory

**Confirmed, High.** Task 4 changes `enigma256FragUkw` from nibble-swap/XOR `0x55` to `x ^ 1`, adds `centerMode`, and conditionally bypasses the reserved pair (`Sources/HELUTToolKit/Enigma256TensorLUT.swift:210-235`). `e256_round1.v` retains the old UKW and labels itself “pre-schema-3” and “not the live reserved-pair center” (`e256_round1.v:3-6`), yet C39 still says it has “sboxes + UKW matching `enigma256ScrambleFrag`” (`directives/claim-sheet.md:92`). The historical cone and current symbol no longer implement the same function.

**Confirmed, High.** `Scripts/enigma256_scramble_frag_battery.sh` synthesizes current `enigma_256_scramble_frag_combo.v` but writes `g5` names and `"generation": 5` (`:16-29`, `:45-60`). `Scripts/enigma256_rb_campaign.sh` similarly routes current v2/gen0 netlists to `*-g5-hard.log` (`:58-69`). The inner TensorLUT log now carries the correct compatibility tuple, leaving two conflicting identities for the same run. C39 must remain a historical Metal SING receipt, and these scripts must use profile-derived labels before E256-003 is marked closed.

## Validation run

`swift test -c release --filter Enigma256Tests` passed 43/43 tests; `swift test -c release --filter testEnigma256FormalCertificate` passed 1/1. The deterministic NLFF emitter, independent LFSR/NLFF RTL checks, five-cone Yosys synthesis, TensorLUT re-emission, twice-promoted golden comparison, direct RTL, directed center vectors, AXIS, and AXI-Lite all passed.

</details>

<details>
<summary>File map</summary>

- `Sources/HELUTCore/Enigma256.swift`, `Enigma256Generation.swift`, `Enigma256Session.swift` — schema-3 center, profile identity, same-mask transition, and immutable capture.
- `Sources/HELUTCore/Enigma256Bridge.swift`, `Fixtures/enigma256_golden/**` — branch-covering 1,024-byte KAT, staged promotion, traces, and manifest.
- `enigma_256_core.v`, `enigma_256_tb.v`, `enigma_256_center_tb.v`, `enigma_256_axi_tb.v` — live center and direct/directed/wrapper parity.
- `Sources/HELUTToolKit/Enigma256TensorLUT.swift`, `enigma_256_scramble_frag_combo.v`, `enigma_256_*tensorlut.v` — revised Red fragment and profile/source-stamped derivatives.
- `Tests/HELUTTests/Enigma256Tests.swift`, `Sources/HELUTCore/Enigma256Formal.swift`, `logs/e256-v2-gen0-center-equality.json` — reciprocity, vectors, calibration, control, and scope.
- `e256_round1.v`, `directives/claim-sheet.md`, `Scripts/enigma256_scramble_frag_battery.sh`, `Scripts/enigma256_rb_campaign.sh` — contradictory historical/current identities requiring correction.

Full scoped diff: `git diff main -- Fixtures/enigma256* Sources/HELUTCore/Enigma256* Sources/HELUTToolKit/Enigma256* Tests/HELUTTests/Enigma256Tests.swift e256_round1.v enigma_256* Scripts/enigma256*`; use `git diff --no-index /dev/null <path>` for untracked task-4 artifacts.

</details>
