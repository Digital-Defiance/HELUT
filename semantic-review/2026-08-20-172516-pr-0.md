# Schema-3 reciprocal center with incomplete profile and theorem sync

Task 4 now implements one coherent schema-3 reciprocal center across Swift, hand RTL, the Red fragment, generated derivatives, and a branch-covering 1,024-byte KAT. The prior pass’s two blockers are resolved: C39 is explicitly historical pre-schema-3 evidence, while the scramble-fragment battery and wide campaign derive `v2/gen0/fixture3/2a9f54…` labels from the fixture instead of writing `g5`. Functional validation is clean, but another active battery still writes generation-5 provenance, the public E256 identity still presents gen5/cubic6 as live, and C24 still names a lemma and API removed by this diff.

Watch for: **confirmed, High** `enigma256_red_battery.sh` still relabels current v2/gen0/schema-3 runs as g5; **confirmed, High** the E256 spec and public hub/journal contradict the corrected schema-3 registry; **confirmed, High** C24 and its teaching surfaces still promise `checkFailClosedCoupling`/`hardenedCubic()` although the certificate now checks `nativeProfileIntegrity`.

**Verdict**: NEEDS_CHANGES

## High-level view

The schema-3 executable profile is internally consistent. Profile hash `2a9f54…` binds suite 2, generation 0, fixture schema 3, the reserved-pair center rule, pre-step parity mode, map order, recurrence, and native NLFF; Swift and RTL use the same captured mask for the center and subsequent state advance.

The historical boundary is now honest where the prior review asked for repairs. C39, `e256_round1.v`, REPRODUCE, the textbook claim snapshot, and the external FHE episode all call the 58-LUT cone historical pre-schema-3 evidence, while both named task-4 scripts derive current tuple labels from `Fixtures/enigma256_generation.json`.

The remaining profile identity is split across active entry points and public surfaces. The new registry says E256-v2/gen0/schema 3 is live, but another advertised Red battery emits g5 metadata and the E256 spec, hub, and journal call gen5/cubic6 the live field and runtime mutation the active workflow.

The formal certificate changed behavior without moving its claim contract. Code replaces the fifth fail-closed-coupling lemma with native-profile integrity and schema-3 center hypotheses, but the claim sheet, theorem statement, reproduction prose, application gallery, and textbook still describe the deleted coupling rollback.

<details>
<summary>Issues (3)</summary>

1. **Current NLFF battery emits g5 provenance** (**confirmed, High**) — `enigma256_red_battery.sh` runs the current fixture-backed v2/gen0 cone but writes g5 filenames and `"generation": 5`; derive the full tuple from the fixture or make the runner historical-only.
2. **Public live-profile identity is stale** (**confirmed, High**) — the E256 spec, project page, and journal advertise live gen5/cubic6 and runtime mutation despite the v2/gen0/schema-3 registry and fail-closed offline-promotion code; synchronize those public surfaces and quarantine gen5 wording.
3. **C24 describes a removed lemma** (**confirmed, High**) — the certificate now checks `nativeProfileIntegrity`, while the sheet/theorem/reproduction/gallery/textbook still cite coupledCubic6 rollback and a nonexistent API; update C24 first, then rebuild every teaching surface.

</details>

<details><summary>Details</summary>

## Profile-bound center and 1,024-beat KAT

Mode is the XOR parity of the four current NLFF enables: mode zero applies the fixed-point-free base reflector, while mode one bypasses only reserved center inputs zero and one. `processTraced` captures that pre-step mask once, uses it for the center, then advances offsets and the LFSR with the same mask; the hand core and scramble fragment use the same ordering.

The fixture, session, manifest, tablebench parameters, equality receipt, and checked-in TensorLUT derivatives agree on `E256/v2/gen0/2a9f54c70a1619805a911758158f1e2204b0fd96c35102a9db5f4575aeb40cb0/fixture-v3`. All eight trace streams have 1,024 rows and valid manifest hashes, covering `(mode,input)` pairs `0:0`, `0:1`, `1:0`, and `1:1`. The equality receipt reproduces 4,083/1,048,576 equal zero-plaintext bytes (z = 0.204) and a deliberately broken fixed-point control at 100%; it explicitly disclaims IND-CPA proof, a security estimate, external cryptanalysis, unbreakability, and production readiness.

## Remaining NLFF battery writes generation-5 provenance

**Confirmed, High.** `Scripts/enigma256_red_battery.sh` synthesizes `enigma_256_nlff_combo.v`, and `runEnigma256TensorLUT()` bootstraps `Fixtures/enigma256_generation.json`, so it now evaluates v2/gen0/schema 3. It nevertheless writes `enigma256-red-battery-g5.jsonl`, `tensorlut-battery-g5-*`, `enigma_256_tensorlut_g5_*`, and `"generation": 5` (`Scripts/enigma256_red_battery.sh:2-3,11,17-18,27,45`). The script predates this diff, but changing the fixture it consumes makes the mislabel a consequence of the profile migration.

`Enigma256.md` and application-gallery slot III.2 still advertise this entry point. Derive suite, generation, schema, hash, compatibility key, filenames, and ledger fields from the fixture as the repaired scramble-fragment battery does, or quarantine this runner so it cannot consume current sources.

## Public E256 identity contradicts the live registry

**Confirmed, High.** `site/src/projects/registry.ts:55-68` correctly publishes “E256-v2/gen0 · schema 3,” the 1,024-byte parity receipt, and historical gen5/C39 separation. The public project page still labels the live field “Gen 5,” describes bred cubic6 taps, says the Red loop mutates generation labels at runtime, and presents the gen5 battery as live (`site/src/pages/Enigma256Page.tsx:32,126-133,284-285,439,475,505,607-610`). The journal similarly opens with `GEN 5` and says Blue rolls fixture genes under pressure (`site/src/pages/Enigma256JournalPage.tsx:25-37`); `Enigma256.md:147,194-218,246-248` publishes the same obsolete workflow.

Those statements conflict with the fixture’s generation-0/native-reversible profile and `runEnigma256Campaign()`, which rejects runtime mutation in favor of deterministic offline search/promotion. Update the canonical E256 spec plus hub/journal diagrams and chronology to v2/gen0/schema 3, describe gen5 as quarantined historical evidence, and remove active mutation claims.

## C24 still promises a removed coupling-harden lemma

**Confirmed, High.** `Enigma256Formal` replaces `.failClosedCoupling` and `checkFailClosedCoupling()` with `.nativeProfileIntegrity` and `checkNativeProfileIntegrity()`; its hypotheses now bind the schema-3 path, reserved base pair, 0/2 center fixed points, pre-step parity, and frozen profile identity (`Sources/HELUTCore/Enigma256Formal.swift:15-22,134-180`). C24 still says its fifth lemma is “coupledCubic6 reject,” `directives/enigma256-theorem.md` invokes `hardenedCubic()`, REPRODUCE repeats that contract, and gallery slot III.3 points to nonexistent `Enigma256Formal.checkFailClosedCoupling`.

Move C24’s canonical wording first, then synchronize `directives/enigma256-theorem.md`, `REPRODUCE.md`, `directives/application-gallery.md`, `site/src/pages/AppsPage.tsx`, and the Pillar III textbook sources/generated outputs. Keep the replacement’s bounded structural scope rather than upgrading native-profile integrity into a universal proof.

## Validation and claim scope

`swift test -c release --filter Enigma256Tests` passed 43/43, including the reproduced equality count; `testEnigma256FormalCertificate` passed 1/1. Temporary golden generation was byte-identical through initial promotion and repeated replacement. Direct RTL matched all 1,024 ciphertext bytes and every state trace; directed vectors forced both modes and both reserved inputs; AXIS and AXI-Lite each loaded 2,560 table bytes and matched all outputs.

The deterministic NLFF emitter reported current artifacts, all five Yosys cones synthesized, derivative source/profile hashes matched fresh netlists, both repaired scripts passed `bash -n`, the independent fixture/manifest audit passed, and the site built. All four external episode JSON files carry `2026-08-20 / C69`; episode 2 explicitly calls C39 historical pre-schema-3 evidence and not the live center. No task-4 evidence claims unbreakability, production security, an IND-CPA/confidentiality proof, or external cryptanalysis.

</details>

<details>
<summary>File map</summary>

- `Sources/HELUTCore/Enigma256*.swift`, `enigma_256_core.v`, `enigma_256_*tb.v` — schema-3 center, captured profile, traces, and parity.
- `Fixtures/enigma256_generation.json`, `Fixtures/enigma256_golden/**`, `logs/e256-v2-gen0-center-equality.json` — identity, KAT, manifest, and equality receipt.
- `Sources/HELUTToolKit/Enigma256TensorLUT.swift`, `enigma_256_scramble_frag_combo.v`, `enigma_256_*tensorlut*.v` — current Red fragment and generated stamps.
- `directives/claim-sheet.md`, `e256_round1.v`, `REPRODUCE.md`, `textbook/**`, external episode 2 — corrected historical C39 boundary.
- `Scripts/enigma256_scramble_frag_battery.sh`, `Scripts/enigma256_rb_campaign.sh` — corrected fixture-derived labels.
- `Scripts/enigma256_red_battery.sh`, `Enigma256.md`, E256 hub/journal — remaining generation-5 provenance and identity drift.
- `Enigma256Formal.swift`, C24 theorem/gallery/textbook surfaces — changed certificate versus stale public contract.

Full scoped diff: `git diff main -- Fixtures/enigma256* Sources/HELUTCore/Enigma256* Sources/HELUTToolKit/Enigma256* Tests/HELUTTests/Enigma256Tests.swift e256_round1.v enigma_256* Scripts/enigma256* directives/claim-sheet.md directives/enigma256-theorem.md REPRODUCE.md site/src/pages/Enigma256* site/src/pages/AppsPage.tsx site/src/projects/registry.ts textbook`; use `git diff --no-index /dev/null <path>` for untracked task-4 artifacts.

</details>
