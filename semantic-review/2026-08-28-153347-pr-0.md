# E256-v3 freeze and executable parity boundaries

This re-review finds all eight findings from `semantic-review/2026-08-28-121159-pr-0.md` resolved or honestly bounded. The fixes bind fixture artifacts across Swift/Rust, fail closed across RTL/MMIO configuration, align domain and JSON acceptance, and replace self-referential evidence with verifier-gated traces and complete SHA-512 answers. No newly introduced high- or medium-severity correctness flaw was found in scope.

Watch for: **confirmed** MMIO atomicity remains a conformance requirement on a future concrete sink, not proof of deployed hardware behavior; **confirmed** all 13 negative vectors are explicitly declarations, while four exact in-memory controls execute in Swift. E256-003 remains **OPEN**: local parity is not a security proof or external acceptance, and standard AEAD remains mandatory.

**Verdict**: APPROVED

## High-level view

Fixture-v5 now freezes the ordered path/logical-ID/encoding sequence before artifact decoding in both consumers. Swift’s rotor-domain syntax is byte-exact, while Rust reconstructs Swift’s canonical JSON bytes before typed decoding.

The RTL wrapper requires a successful nonzero state load, disarms after rejection, and suppresses advancement/output while unarmed. The Swift plan adds begin/stage/commit/abort semantics, with injected failures preserving the modeled active configuration.

The RTL runner verifies the selected fixture before simulation. Its 1,024-beat bench checks verifier-backed state, masks, center path, output, and post-state; declaration counts and cryptographic KATs now describe only evidence they execute.

<details>
<summary>Issues (0)</summary>

No unresolved actionable finding in the requested scope.

</details>

<details>
<summary>Details</summary>

## Canonical fixture and parser contracts now agree

Finding 1 is resolved. Swift compares the exact ordered artifact tuple sequence before reading descriptors (`Sources/HELUTCore/Enigma256FixtureV5.swift:279-335`, `:625-627`), and Rust freezes the same sequence (`Reference/Rust/src/e256_v3.rs:478-547`). Remap and descriptor-backed-extra mutations exercise the boundary (`Tests/HELUTTests/Enigma256FixtureV5Tests.swift:231-314`).

Findings 4 and 5 are resolved. Swift requires two ASCII decimal suffix bytes and an index below 16 (`Sources/HELUTCore/Enigma256V3.swift:188-207`); signed, padded, short, and out-of-range aliases are tested (`Tests/HELUTTests/Enigma256V3Tests.swift:49-64`). Rust reconstructs Swift’s sorted pretty JSON with one LF and requires byte equality before typed decoding (`Reference/Rust/src/e256_v3.rs:411-476`, `:559-563`), with reordered-key and whitespace controls at `Reference/Rust/src/e256_v3.rs:1578-1589`.

## Configuration fails closed at both boundaries

Finding 2 is resolved. `configuration_armed` gates core input and wrapper output; reset leaves it false, a zero-state load errors and disarms, and only a valid load re-arms (`Hardware/RTL/Enigma256/enigma_256_core_v3.v:36-67`). The bench covers reset-before-load, retained state after rejection, rejected payloads without advancement, and re-arm (`Hardware/Testbenches/Enigma256/enigma_256_v3_tb.v:118-151`, `:173-244`).

Finding 3 is resolved at the programming-contract boundary. The sink requires inactive staging, atomic commit, idempotent abort, and no activation on a throwing commit (`Sources/HELUTCore/Enigma256V3MMIO.swift:17-46`); `program(into:)` aborts every failed transaction (`Sources/HELUTCore/Enigma256V3MMIO.swift:122-155`). Injection covers begin, table edges/boundary, state, and commit while retaining the old active token (`Tests/HELUTTests/Enigma256V3MMIOTests.swift:196-276`). No concrete production sink lands here, so this proves caller sequencing and the conformance contract, not hardware realization.

## Evidence gates execute without overstating scope

Finding 6 is honestly bounded. Reports and CLI call all 13 entries declarations (`Sources/HELUTCore/Enigma256FixtureV5.swift:263-266`, `Reference/Rust/src/e256_v3.rs:30-35`, `Reference/Rust/src/main.rs:65-75`). Swift separately executes four controls with exact associated-error matches (`Sources/HELUTCore/Enigma256FixtureV5.swift:955-1001`); the other declarations are not presented as executed evidence.

Finding 7 is resolved. The runner invokes `e256-v3-kat --bundle` on the selected directory before simulation (`Scripts/enigma256_v3_sim.sh:17-21`, `Reference/Rust/src/main.rs:65-75`). Rust independently regenerates and byte-compares the trace (`Reference/Rust/src/e256_v3.rs:987-1035`); RTL checks pre-state, step/center masks, center path, output, and post-state for every row (`Hardware/Testbenches/Enigma256/enigma_256_v3_fixture_tb.v:125-204`). The v2/v3 include comparison is therefore supplementary, not the sole NLFF oracle.

Finding 8 is resolved. Rust compares the complete RFC 4231 HMAC-SHA512 result and complete 42-byte HKDF-SHA512 output (`Reference/Rust/src/e256_v3.rs:1551-1575`); an independent standard-library calculation reproduced both constants.

## Scoped validation

`swift test --filter 'Enigma256(V3|FixtureV5)'` reported 19 tests, zero failures, and one opt-in emitter skipped. Three scoped Rust unit tests passed; the exact staged verifier reported 1,024 bytes, 26 artifacts, 64 recurrence rows, and 13 declared negatives. Both RTL benches passed, including `1024/1024` fixture beats. The shell adapter returned status 1 even for `true`, so these receipts use completed test/bench output rather than its status field.

</details>

<details>
<summary>File map</summary>

- `Sources/HELUTCore/Enigma256V3.swift` and its tests — indexed-domain acceptance.
- `Sources/HELUTCore/Enigma256FixtureV5.swift` and its tests — bindings, declaration naming, controls, and trace authoring.
- `Sources/HELUTCore/Enigma256V3MMIO.swift` and its tests — transaction contract and fault injection.
- `Reference/Rust/src/e256_v3.rs`, `Reference/Rust/src/main.rs`, and parity tests — canonical JSON, bindings, verifier CLI, trace rederivation, and KATs.
- `Hardware/RTL/Enigma256/enigma_256_core_v3.v`, v3 benches, and `Scripts/enigma256_v3_sim.sh` — arm/disarm and verifier-backed RTL checks.

Prior findings/full original scope: `semantic-review/2026-08-28-121159-pr-0.md`.

</details>
