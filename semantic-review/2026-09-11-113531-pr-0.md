# Exact frozen execution and per-input timing evidence for E256-H H2

Both prior High findings are resolved: each fixed variant now executes only its 24 frozen H4 inputs, while each direct variant executes all 9,216 inputs, with exact identities enforced rather than totals alone. Ready/busy windows are checked for every one of the 27,720 executed inputs, so initiation interval and input independence are no longer singleton extrapolations. The regenerated receipt and banking surfaces agree on digest `b0b62be2cfebd870f71ce0b5e5300eb52ab8c2aea243712d1617f4e2351dc7e1`, and the accounting caveat, no-selection/no-claim scope, and reviewed-standard-AEAD boundary remain explicit. The full frozen reproduction ends in `CHECK PASS`.

Watch for: no actionable behavioral or evidence defects remain (**confirmed**); inherited timescale warnings are outside the requested scope.

**Verdict**: APPROVED

## High-level view

Execution eligibility is now gated before `start`: all six variants run on the 24 frozen inputs, but only direct variants run elsewhere. The runner independently rejects unexpected fixed output and requires the exact preregistered input set for every variant, closing both hidden-work and emit-filter loopholes.

Timing evidence is transaction-wide. Each selected input has a checked busy window through completion, and acceptance requires one observation for every expected variant/input pair.

The receipt remains an evidence-only H2 comparison. Its cost deltas retain the whole-flow/ABC limitation, it makes no architecture selection or claim movement, and it does not weaken the requirement for reviewed standard AEAD in deployment.

<details>
<summary>Issues (0)</summary>

No actionable findings remain.

</details>

<details><summary>Details</summary>

## Start gating closes the hidden fixed-work gap

The testbench uses all six `run_mask` bits only for fixed H4 inputs and the three direct bits otherwise, then asserts `start` only for selected variants (`Hardware/Testbenches/Research/E256H/e256h_h2_integrated_core_tb.v:404-411`). This prevents work rather than hiding output: the core admits work through `start_accept = start && ready`, including the FF, ring, and memory paths (`Hardware/RTL/Research/E256H/e256h_h2_integrated_core.v:303,521-525,625-628,760-765`).

The runner rejects output in an unexecuted fixed slot, requires 24 outputs per fixed variant and 9,216 per direct variant, and compares actual `(config_index, block_index)` pairs with each exact expected set (`Scripts/e256_hardware_h2_gate.py:1022,1188,1232-1243`). The receipt records those populations and exact-set success (`logs/e256-hardware-h2-gate.json:453-459,613-625,706`). Together, these checks remove the prior 9,192 hidden extra fixed executions per top rather than merely suppressing their receipt rows.

## Per-input windows close the singleton timing gap

Every selected variant/input pair rejects premature `ready`, dropped `busy`, completion without `ready`, and completion with `busy` asserted; latency and earliest reacceptance are recorded from that window (`Hardware/Testbenches/Research/E256H/e256h_h2_integrated_core_tb.v:433-445`). The runner requires each variant's cycle-observation count to equal its expected output count, singleton measured latency/initiation-interval sets, and a global count of 27,720; H2P5 also binds the observations to exact input sets and checked windows (`Scripts/e256_hardware_h2_gate.py:2449-2464,2554-2560,2574-2577`).

The receipt contains 24 observations for each fixed variant and 9,216 for each direct variant, totaling 27,720, with `per_input_ready_window_checked`, `variant_input_sets_exact`, and `exact_execution_sets` asserted (`logs/e256-hardware-h2-gate.json:600-612,706`). Timing claims are therefore graded from every executed transaction at both capture and receipt-validation boundaries.

## Canonical evidence retains its interpretation boundaries

An independent canonical-payload recomputation matches the stored digest at `logs/e256-hardware-h2-gate.json:6839`, and the receipt's gate-artifact hashes match the current RTL, testbench, and runner. The architecture note, audit, and reproduction guide bank the same digest and H2 verdict (`directives/e256-hardware-architecture.md:800-863`; `directives/e256-audit.md:333-342`; `REPRODUCE.md:792-802`); no active banking surface contains the superseded digest.

Recursive accounting and flattened-count preservation remain checked (`logs/e256-hardware-h2-gate.json:709-711`). Direct-minus-fixed numbers remain whole-flow generic synthesis observations because ABC runs alternatives separately, not isolated physical selector costs (`logs/e256-hardware-h2-gate.json:4272-4275`; `directives/e256-hardware-architecture.md:850-853`; `directives/e256-audit.md:338-342`). The receipt still records `production_architecture_selected=false`, `claim_movement=false`, and null production decisions (`logs/e256-hardware-h2-gate.json:4221-4242`), while the banked surfaces preserve the reviewed-standard-AEAD requirement (`directives/e256-hardware-architecture.md:862-863`; `directives/e256-audit.md:342`; `REPRODUCE.md:799-802`; `textbook/chapters/pillar-iii.tex:28-32`).

## Frozen gate and rejection probes pass

`make e256-hw-h2-check` checked 9,216 functional rows, 27,720 model comparisons, 18,480 storage comparisons, 72 bridges, and all 12 controls. It reproduced all six cost tuples, the no-selection verdict, and the canonical digest before `CHECK PASS`. Separate negative probes confirmed that an extra fixed output and a missing cycle observation are each rejected.

</details>

<details><summary>File map</summary>

- `Hardware/Testbenches/Research/E256H/e256h_h2_integrated_core_tb.v` — gates starts and captures per-input ready/busy windows.
- `Hardware/RTL/Research/E256H/e256h_h2_integrated_core.v` — ties variant work to accepted starts.
- `Scripts/e256_hardware_h2_gate.py` — enforces exact execution sets, timing counts, and receipt integrity.
- `logs/e256-hardware-h2-gate.json` — regenerated H2 evidence and canonical digest.
- `directives/e256-hardware-architecture.md` — banks scope, synthesis caveat, and AEAD boundary.
- `directives/e256-audit.md` — records the audited digest and bounded interpretation.
- `REPRODUCE.md` — preserves the frozen command and deployment limits.
- `textbook/chapters/pillar-iii.tex` — retains the no-selection hedge.

Full local change view: `git diff main` plus the untracked H2 tranche shown by `git status --short`.

</details>
