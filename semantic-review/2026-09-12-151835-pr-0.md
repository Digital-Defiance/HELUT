# Proof-carrying H4 transition graph and bounded X0 path certificate

The tranche derives the complete 384-node H4 safe-transition graph, reconstructs the predecessor arithmetic census, and emits a deterministic twelve-selector path certificate with synchronized OPEN-only documentation. Independent reconstruction matched the pair counts, graph bytes/root, topology, walk counts, prior witness, receipt hashes, and pinned identities; both reproduction entry points pass. One frozen control does not perform its declared mutation, yet its manufactured rejection feeds harness validity and every downstream “23/23” statement. **Watch for: High, confirmed — `filtered_pair_corpus` compares two hard-coded counts instead of filtering the production pair corpus, so shared-path control coverage is not established.**

**Verdict**: NEEDS_CHANGES

## High-level view

Literal 32-lane propagation and a separate modular-support implementation agree that `[x,y,z]` is all-to-all exactly when `S(y)+S(z)=Z8`; `x` only translates each original row’s reachable columns. That law drives the canonical LSB-first 18,432-byte graph. Its two closed components force the exact Hamiltonian search negative, so the frozen fallback emits the first support-changing two-cycle, `[0,3]`, with 11 verified transitions.

The receipt separates a canonical deterministic payload from an advisory envelope. `--check` validates the archived record, regenerates the payload, and compares canonical payload bytes and digest; the outer record hash remains archival because elapsed time and repository state are advisory inputs. Preregistration, markers, predecessor artifacts, implementation source, and Make entry points match their banked identities.

The **confirmed** blocking asymmetry is confined to control 18. The production census directly traverses all 147,456 pair identities, but the control never runs that path with one identity missing; it asks the generic equality helper to reject `147455 != 147456`, which guarantees a green control without testing corpus filtering.

All requested prose surfaces preserve the adverse two-component finding and the OPEN boundary. They make no mixed-wide-trail, attack-resistance, hardware, production-policy, self-evolving-cipher, or C/H/N claim.

<details>
<summary>Issues (1)</summary>

1. **High — synthetic filtered-corpus control (confirmed)** — `filtered_pair_corpus` manufactures a count mismatch instead of removing a pair through the production census validator. Route the actual census and a genuinely one-pair-short corpus through the same identity/coverage validation, regenerate the receipt, and resync derived hashes and “23/23” assertions.

</details>

<details>
<summary>Details</summary>

## Filtered-pair control never filters a pair — High, confirmed

The frozen contract requires controls to traverse the same validation paths and defines control 18 as removing one pair from the exhaustive corpus (`directives/e256-x0-h4-evolution.md:72`, `directives/e256-x0-h4-evolution.md:91`; `directives/e256-x0-h4-evolution-preregistration.json:187`). The production path is the direct 384×384 census at `Scripts/e256_x0_h4_evolution_gate.py:715-740`. The control instead executes only:

```python
lambda: require_equal(PAIR_COUNT - 1, PAIR_COUNT, "ordered algebraic pair coverage")
```

at `Scripts/e256_x0_h4_evolution_gate.py:1418-1421`. `expect_rejection` necessarily records that preconstructed inequality as detected; the aggregate promotes it into `all_twenty_three_controls_detected` and overall harness validity (`Scripts/e256_x0_h4_evolution_gate.py:1463`, `Scripts/e256_x0_h4_evolution_gate.py:1582-1584`). The receipt banks the synthetic rejection while asserting success (`logs/e256-x0-h4-evolution-gate.json:1474-1478`, `logs/e256-x0-h4-evolution-gate.json:2395-2397`).

The nested loop still establishes actual pair coverage, but the stronger shared-path “23/23” claim is unsupported. Factor exact pair membership, uniqueness, and count into the production coverage validator, run the normal census through it, then pass the same validator a corpus with one real `(i,j)` omitted. After regenerating, resync the source/payload/raw/record hashes and revalidate the affected assertions at `directives/e256-x0-h4-evolution.md:113-123`, `directives/e256-hardware-architecture.md:954-957`, `directives/e256-audit.md:129` and `directives/e256-audit.md:366`, `REPRODUCE.md:833-850`, and `textbook/chapters/pillar-iii.tex:45-47`. The frozen marker and preregistration already specify the required behavior and should remain unchanged.

## Exact reconstruction of the bounded certificate

The independent set-based model reproduced exact literal/closed-form agreement for all 147,456 ordered pairs and all 98,304 first-selector/support-pair cases, plus 2,048 support translations (`Scripts/e256_x0_h4_evolution_gate.py:647-816`). It recovered 73,728 safe pairs, bitset SHA-256 `33752c5f7a689fbeb031b0a5b7f8e80b0849dc4d8962ce04a74e1f73bb5fe0c3`, and root `d7e44b625acc693a28469dd993edfe3bf8cd7a4b966275a70ad7cf3941e4a80a` under the declared domain/node/catalog/bitset preimage (`Scripts/e256_x0_h4_evolution_gate.py:835-860`). It also reproduced two 192-node graph SCCs, two 8-node quotient SCCs, no Hamiltonian cycle, and exact walk counts 73,728; 14,155,776; 2,717,908,992; and 5,019,318,332,045,454,244,023,631,872 for lengths 1, 2, 3, and 11.

The prior map `(b,d) -> (b+d,b+2d)` closes with inverse `(2i-j,j-i)` over all 147,456 classes, yielding 73,728 failed classes, 737,280 placements, zero-failure stride `[0]`, and the original `[1,2,3]` witness (`Scripts/e256_x0_h4_evolution_gate.py:1115-1191`). Path selection and verification bind twelve indices and tuple codes to the reconstructed graph/root and check all eleven edges (`Scripts/e256_x0_h4_evolution_gate.py:1194-1280`). The resulting alternating path stays in one component and certifies only its represented three-round structural windows.

## Integrity and deterministic-check boundary

Independent hashing matched source `88888fb9435d10c155317ac27fa98ff0e82f7843992dddd798637533c2cecf1a`, preregistration `d39759bc2677fa17227ea129fc3ec20791a749cec2b9cdd33aa18c45df338711`, X0 marker `1ef354e9b933838eb3e74b32b202e4693f1bfdd62379f10dbc6a8673b3383962`, H4 marker `aac50bda525ead96234fc3c0e3e3692fabbde101de34d44f632d6a65bfbed1e4`, deterministic payload `d56a4a97d17711e19da5c8e64542382a84560dbca0918aa152a37979ef5653c1`, archived record `c9d308a36c96d179ee707d3539a964581e96932276200845b40365bc7cdb12ec`, and raw receipt `403fcf188003dbe3344a5897049b6afb833d19fdd62295983dfd48778bea56c2` (`Scripts/e256_x0_h4_evolution_gate.py:37-50`, `Scripts/e256_x0_h4_evolution_gate.py:496-604`, `Scripts/e256_x0_h4_evolution_gate.py:1645-1713`). `make e256-x0-h4-evolution-check` and `python3 Scripts/e256_x0_h4_evolution_gate.py --self-test` pass under Python 3.9.6; the Make recipes are at `Makefile:285-294`.

</details>

<details>
<summary>File map</summary>

- `Scripts/e256_x0_h4_evolution_gate.py` — derives and verifies the graph, diagnostics, path, controls, and receipt.
- `directives/e256-x0-h4-evolution.md` — freezes the contract and banks the bounded result.
- `directives/e256-x0-h4-evolution-preregistration.json` — freezes identities, scopes, controls, verdict, and non-claims.
- `logs/e256-x0-h4-evolution-gate.json` — archives deterministic evidence and the advisory envelope.
- `Makefile` — adds emit and reproduction-check targets.
- `directives/e256-hardware-architecture.md` — adds the §12 X0 capsule.
- `directives/e256-audit.md` — keeps E256-063 OPEN and records X0 progress.
- `REPRODUCE.md` — documents expected outputs and boundaries.
- `textbook/chapters/pillar-iii.tex` — adds the OPEN scaffold summary.

Full scoped tracked diff: `git diff main -- Makefile REPRODUCE.md directives/e256-audit.md textbook/chapters/pillar-iii.tex`. The core X0 files and `directives/e256-hardware-architecture.md` are untracked, so their full contents are the local additions under review.

</details>
