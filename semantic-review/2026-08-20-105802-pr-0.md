# Durable single-owner lifecycle for the P1030680 campaign ledger

The campaign ledger now establishes ownership with one atomic, non-truncating open and keeps that descriptor locked across recovery, evaluator work, durable append, and teardown. This closes both blocking findings from the previous pass without changing the previously confirmed protocol-v3 replay and resume behavior. The supplied post-initialization and post-first-chunk Python probe returning `BLOCKED` agrees with the descriptor lifetime visible in the source. No remaining actionable correctness or safety issue was found in the scoped lifecycle.

Watch for: No remaining actionable ledger-ownership concern; both prior races are **confirmed closed**.

**Verdict**: APPROVED

## High-level view

First creation no longer has a check-then-truncate window. `Darwin.open` combines lookup and creation with `O_RDWR | O_CREAT` but no `O_TRUNC`, then nonblocking `lockf` ownership is acquired before any ledger read, repair, or append.

All ledger I/O uses the wrapped descriptor. It transfers into the ledger object only after recovery and validation succeed, remains reachable for pending appends, and is closed on initialization failure or teardown; no URL-backed ledger read remains to trigger Darwin's process-scoped close behavior.

<details>
<summary>Issues (0)</summary>

No actionable correctness or safety issues found in this scoped pass.

</details>

<details><summary>Details</summary>

## Atomic creation closes the pre-ownership truncation window

The ledger opens the path once with `O_RDWR | O_CREAT` and mode `0644` at `Sources/HELUTToolKit/P1030680MuleinCampaign.swift:222-225`. Without `O_TRUNC`, a delayed first-time runner cannot erase a ledger another runner created in the interim. Cooperative runners receive descriptors for the same inode, and the first successful `F_TLOCK` at `:233` owns it before either performs content I/O; a loser closes its descriptor without mutation at `:233-240`.

## One descriptor carries ownership through recovery and append

After locking at offset zero, initialization reads, repairs a torn tail, synchronizes, and seeks to EOF through `lockedHandle` (`:253-273`, `:326-333`). Newline repair and every append also write and synchronize through the retained handle (`:330-334`, `:347-374`). The removed URL read means no second descriptor is closed inside the owning process, so the earlier Darwin `lockf` self-release path is gone.

Before successful transfer, deferred cleanup unlocks and closes the descriptor; after transfer, `self.handle` owns it until `deinit` closes it (`:242-248`, `:325-345`). The `ledger` reference is required after each evaluation for `ledger.append`, preventing teardown while a chunk commit remains pending. The supplied Python probe returning `BLOCKED` after initialization/resume and after the first chunk corroborates both retention points.

## Protocol-v3 admission remains intact

The ownership patch surrounds the existing v3 loader and append protocol. The previously confirmed ordered-prefix validation, complete-receipt commitment, candidate replay, tail recovery, prior-gate halt, manifest snapshot, and post-gap admission paths remain unchanged; locking now precedes loaded-row validation and repair, while append still validates the next plan element before durable write.

The source compiled during `swift build`, and `swift test --filter MuleinFutureTests` reported 10/10 selected tests passing. The execution tool reported status 1 despite the successful build/XCTest transcript (and did the same for `/bin/date`), so validation relies on the emitted summaries plus the supplied two-point contention probe.

</details>

<details>
<summary>File map</summary>

- `Sources/HELUTToolKit/P1030680MuleinCampaign.swift` — atomically opens, exclusively locks, recovers, appends, and releases the protocol-v3 JSONL ledger through one descriptor.

Full local view: `git diff --no-index /dev/null Sources/HELUTToolKit/P1030680MuleinCampaign.swift` (the file is untracked, so `git diff main` has no entry).

</details>
