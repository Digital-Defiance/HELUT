# E256-vNext bake-off executes its frozen rejection contract

This follow-up refreezes the E256-vNext rotor/schedule campaign and makes the preregistration, boundary corpus, controls, baselines, serialization, authorization binding, and fallback policy executable. All seven blockers from `semantic-review/2026-09-07-200027-pr-0.md` are resolved through the same receipt-building and interpretation path used for candidate selection. `make e256-vnext-bakeoff-check` recomputed the complete payload byte-for-byte at `fd54809cbd12de9d6f14151bcd401232aae765b72cb8368128ea54bdfa7b7197`.

Watch for: **confirmed** this remains an OPEN model-only rejection receipt; it selects no security depth and authorizes no cipher/profile or production implementation.

**Verdict**: APPROVED

## High-level view

The authenticated preregistration now supplies the rotor domains and campaign contract, while exact validation rejects implementation/declaration drift. Both required counter boundaries and a separate sequence-reset transition are enforced per cell before interpretation.

All eight planted defects reach shared observation, summarization, and rejection machinery wherever applicable. The ideal-independent null and clean domain checks use the same interpretation functions, while PRF-mask-only and AEAD-only execute the Q4 comparison contract.

Effective-state identities serialize only the active rotor prefix. D0–D11 are bound through frozen definitions and a digest limited to the architecture authorization section, avoiding recursion through mutable result prose.

Selection ranks schedule, family, and numeric depth explicitly and self-tests each fallback. The interpretation gate refuses emission or acceptance on failure; the retained 1+1 affine-AES cell is only the next attack target, with AEAD-only still the production baseline.

<details>
<summary>Issues (0)</summary>

No actionable findings.

</details>

<details>
<summary>Details</summary>

### Frozen domains and boundary coverage now drive selection

`ROTOR_BANK_DOMAINS` comes from the preregistered `rotor/in` and `rotor/out` entries, schedule and mask use their frozen domains, and controls are rooted under `controls`. `verify_frozen_inputs()` authenticates the preregistration, predecessor artifacts, architecture authorization, and runner; exact-record checks reject drift in formulas, IDs, corpus, hard rules, and receipt semantics. An in-memory mutation back to `rotor/ingress` was rejected by this gate.

The twelve-counter corpus includes `65535→65536` and `4294967295→4294967296`. The reset lane independently derives `(sequence=7,counter=65536)` and `(sequence=8,counter=0)`. Per-cell coverage requires 1,536 ordinary maps, 256 reset endpoint maps, seven adjacent pairs, twelve direction pairs, and the reset pair; aggregate dimensions are 46,080 ordinary maps, 7,680 reset endpoint maps, and 3,840 reset comparisons.

### Planted weaknesses and baselines share campaign semantics

Mirrored endpoints, identity/affine maps, duplicate banks, and fixed common endpoints pass through normal map construction before shared observation, summary, and `candidate_rejection_reasons()`. Counter truncation and direction omission use normal state derivation and domain-collision paths; ignored-key-bit uses the full derivation transcript and key-flip path. Only modulo sampling takes its preregistered primitive-level exception.

Exact control IDs and rejection-reason sets are mandatory. All eight controls are detected, the campaign common-endpoint arm passes at every depth, and the ideal-independent null traverses 9,216 ordinary contexts plus 1,536 reset endpoints without exact rejection. Clean candidate paths report no adjacent, reset, direction, or pinned key-flip collision.

PRF-mask-only executes those same 9,216 ordinary contexts and 1,536 reset endpoints, records one-pair repeated-state mask recovery, and accounts for one mask derivation plus one XOR per byte. AEAD-only records zero inner-transform work and marks byte-map/codebook metrics not applicable. Q4 therefore retains no production rotor absent a later independent-key compromise experiment under common cost accounting.

### Active prefixes and D0–D11 avoid mutable hash cycles

`effective_state_bytes()` slices IDs and wrappers at the selected depth and includes active A/B maps and the mask. A targeted probe changed only slot 16: the 1+1 digest remained stable while the 16+16 digest changed; full derivation transcripts still cover all depths for key-flip checks.

The preregistration embeds and hashes the accepted D0–D11 definitions. `architecture_authorization_record()` separately verifies D0 through D11 and hashes only `### 0.1 Accepted research constraints` through the start of `### 0.2 Choices deliberately left OPEN`, producing `d51992b42d7b46c5f9c43b8fb3090a17a7e715daef4df64b6763f2d3e5e98d43`. The mutable bake-off result is in section 2.1, outside that slice, so printing the receipt digest cannot alter its own authorization input.

### Structured fallback and receipt acceptance fail closed

`structured_cell_rank()` orders full-counter schedule, affine-AES family, numeric depth, then declaration order. Self-tests cover preferred-cell rejection and each ranking dimension. The frozen run selects `aes_affine_v1|full_counter_reselect_v1|1+1`; a targeted failed-gate probe instead returned `INVALID_CONTROLS` with no baseline.

Receipt construction requires validated inputs, controls, baselines, campaign/common-endpoint coverage, key-flip checks, and ranking tests. `main()` refuses emit/check when that conjunction fails. The success path compares both digest and complete payload; this run printed `digest_match=True`, `payload_match=True`, and `CHECK PASS`.

### The audit preserves the OPEN boundary

The topology record and audit use the reproduced dimensions and digest, invalidate the superseded run, and describe all 20 candidate cells only as bounded failures to reject. The 255-query repeated-state ceiling, visible—not sealed—holdout, absent depth selection, missing security proofs and external review, and mandatory standard-AEAD boundary remain printed. E256-003, E256-061, and all tranches stay OPEN; `directives/claim-sheet.md` is unchanged, with no C/H/N movement.

</details>

<details>
<summary>File map</summary>

- `Makefile` — exposes campaign generation and read-only reproduction.
- `Scripts/e256_vnext_bakeoff.py` — executes corpus, controls, baselines, ranking, gate, and receipt.
- `directives/e256-vnext-bakeoff-preregistration.json` — refreezes the campaign contract.
- `logs/e256-vnext-rotor-schedule-bakeoff.json` — records the reproduced OPEN payload.
- `directives/e256-vnext-topology.md` — records the result beneath D0–D11 authorization.
- `directives/e256-audit.md` — preserves dimensions, negative result, and non-production boundary.

Full tracked diff: `git diff main`. The script, preregistration, topology record, and receipt are untracked additions, so the complete surface also requires `git status --short`.

</details>
